import{_ as e,X as o,Y as c,a2 as d}from"./framework-43a12551.js";const a="/CrabSAMA-Blog/assets/image-20220527170639933-b6172255.png",i={},r=d('<h2 id="前提场景" tabindex="-1"><a class="header-anchor" href="#前提场景" aria-hidden="true">#</a> 前提场景</h2><p>使用 <code>webpack_dev_server</code> 的 <code>Vue</code> 项目</p><h2 id="调试步骤" tabindex="-1"><a class="header-anchor" href="#调试步骤" aria-hidden="true">#</a> 调试步骤</h2><p>假设有一种需求，想要对引入的 <code>npm</code> 包进行调试，找出它里面的问题或者了解其运行的原理，我们就会涉及到对引入的包进行调试的需求，要解决的思路也比较简单。</p><ol><li><p>使用 <code>Chrome Dev Tool</code></p><p>我们可以直接运行项目，然后到 <code>Chrome</code> 中按 F12 打开 <code>Dev Tool</code>，切换到 <code>Source</code> 标签页。在这里面我们找到通过 <code>Webpack</code> 引入的包：</p><figure><img src="'+a+'" alt="image-20220527170639933" tabindex="0" loading="lazy"><figcaption>image-20220527170639933</figcaption></figure><p>在找到后，以 <code>iview</code> 这个包为例，我们可以看到它的打包方式是将很多个模块打包成一个 <code>js</code> 文件的形式，我们可以通过搜索找到对应的模块，直接打上断点即可调试。</p></li><li><p>使用 IDE</p><p>我们需要知道的一点是，其实我们直接 <code>import XXX from &#39;xxx&#39;</code> 进来的包，本质上是指向了 <code>node_module</code> 下对应的文件夹。在每个标准的 <code>npm</code> 包中，都会有一个 <code>package.json</code> 文件，其中的 <code>main</code> 属性指向了以上述方式引入直接指向的 <code>js</code> 文件，所以其实我们 <code>import</code> 进来的就是这个 <code>js</code> 文件。</p><p>因此我们可以直接杀到他家，在这个 <code>js</code> 文件中打上断点（<code>debugger</code>），在运行过程中就可以直接通过 <code>Dev Tool</code> 看到调试界面啦~（虽然这样还是要用到 <code>Chrome</code> 的 <code>Dev Tool</code>，但是在 <code>IDE</code> 中查看一些东西相对于 <code>Chrome</code> 还是更方便滴）</p></li></ol>',5),p=[r];function t(n,s){return o(),c("div",null,p)}const m=e(i,[["render",t],["__file","debug-npm-package.html.vue"]]);export{m as default};
